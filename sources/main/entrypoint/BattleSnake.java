package entrypoint;

import option.Configuration;

/**
 * BattleSnake spins up the REST server and initializes the logging system.
 */
class BattleSnake {
  /**
   * Starts the application.
   *
   * @param command_line_arguments process arguments given by the user. Will be
   *                               transformed into a {@link Configuration}.
   */
  public static void main(String[] command_line_arguments) {
    System.out.println("START");
  }
}
